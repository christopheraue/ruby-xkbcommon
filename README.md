# Xkbcommon::SymbolToKeys

## Installation

Add this line to your application's Gemfile:

```ruby
gem 'xkbcommon-symbol_to_keys'
```

And then execute:

    $ bundle

Or install it yourself as:

    $ gem install xkbcommon-symbol_to_keys
